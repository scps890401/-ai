# Android 390 × 844 UI 驗證

日期：2026-08-25

在本地開發預覽以 390 × 844 viewport 擷取首頁短頁與長頁。AI 對話框仍是主要入口；附件縮圖、AI plan summary、逐張 job 狀態 rail、LINE 組數完成度、LINE 輸出規格與貼圖貨架都能在同一條手機內容流中呈現。job rail 使用小圓點顯示第 1–8 張，超過 8 張以 +N 張收合，並以暖色／綠色／紅色分別表達待處理、完成與失敗，沒有形成額外設定表格。

手機長頁沒有看到水平溢出；聊天輸入區、素材上傳、生成按鈕與 ZIP 區塊均保持可操作。視覺上資訊密度偏高但符合目前工作室的創作流程，計畫摘要與 job rail 位於聊天後方，不會遮住輸入。截圖使用空貨架初始範例，因此未能在無額度情況下以真實生成 job 狀態驗證每一種顏色；純函式測試已覆蓋 pending／retrying／completed／failed 狀態更新。

已知限制：目前外部影像服務仍可能回傳 usage exhausted，因此 resume 與指定第 N 張修改的實圖 E2E 仍需額度恢復後補測。


## 桌面 1280 × 720 補充

桌面長頁回歸顯示左側 Creator's Desk 導航、中央 AI 聊天與素材工作區、右側 LINE 貨架維持三欄比例。空貨架狀態下，組數選擇、完成度、四類 LINE 輸出規格、ZIP 區塊與範例卡均未被新增 UI 推擠；新增 job rail 只會在已有 plan 或 job state 時出現，因此初始畫面仍保持簡潔。

## 2026-08-26 ZIP 保存功能回歸

Android 390 × 844 長頁面可讀取聊天輸入、計畫／job rail、附件與素材區、角色提示、LINE 規格與貨架；沒有發現 horizontal overflow 或操作按鈕被固定元件遮擋。桌面 1280 × 720 則保持左側導覽、中央聊天工作流及右側貼圖貨架的層級。ZIP 保存後的「重新下載已保存 ZIP」連結只在專案有 ready export 時顯示，因此空白工作台仍維持最小介面。
