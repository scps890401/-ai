# Phase 1 驗證筆記

## 2026-08-26

Landing State 已在桌面預覽中確認：品牌、標語與 Composer 具明確視覺階層，暖紙背景仍維持深墨文字可讀性。輸入測試訊息後，Composer 的送出控制項已由停用狀態切換為「遇青」可送出狀態。

送出測試訊息後，畫面已轉換為 Active Chat State：使用者訊息置右、Composer 固定於底部、Mock Streaming 顯示停止按鈕，完成後正確呈現標題、粗體、列表，以及複製與重新產生控制項。

## Phase 2

全端升級後，首頁已能正常載入並保留 Phase 1 Landing State。已輸入「我想做 8 張搞怪貓咪的貼圖」作為 SSE、意圖辨識與 Tool Calling 的端對端驗證起點。

該請求已完成 UI 狀態轉換與 SSE 錯誤事件處理；前端正確保留使用者訊息並呈現可理解的失敗提示。模型回覆尚未成功，需檢查伺服器端 AI Provider 的串流相容性後重試。

更新為外部 Gemini／OpenAI Router 後，重新整理頁面已成功由資料庫取回先前的「我想做 8 張搞怪貓咪的貼圖」使用者訊息，確認 Thread 與 Message 寫入、瀏覽器 Thread 指標及載入端點可共同運作。

已對同一 Thread 送出「改成 16 張」。前端正確保留兩則使用者訊息、切換生成狀態並回復為可輸入狀態，但尚未接到模型文字輸出；下一步需檢查 Provider Router、模型識別與工具呼叫的伺服器端例外。

已清除瀏覽器測試 Thread 與 Client 識別，並重新確認可回到乾淨 Landing State。這避免先前失敗請求干擾後續端對端驗證。

已再次送出新的「我想做 8 張搞怪貓咪的貼圖」請求。前端仍保持 SSE Loading／Stop 狀態，尚未收到完成或錯誤事件；需查看伺服器端模型呼叫狀態與逾時行為。

以更新後的 Router 重新開啟瀏覽器時，先前的使用者訊息會由持久化資料載入；Composer 可正常接受新的貼圖企劃請求，送出按鈕也會依輸入狀態啟用。

更新後的前端端對端請求已成功：輸入「我想做 8 張搞怪貓咪的貼圖」後，Composer 進入 Stop 狀態並以 SSE 逐段顯示自然語言企劃草稿；回覆完成後可正常顯示 Markdown、複製與重新產生控制項，Composer 也回復為可輸入狀態。

在同一個已持久化 Thread 中接續輸入「改成 16 張」後，AI 成功延續「搞怪貓咪」主題，觸發新的 `plan_sticker_pack` 企劃工具，並透過真實 SSE 串流完成 16 張的自然語言企劃草稿。此結果確認跨訊息 Context、工具呼叫、串流渲染與資料庫對話紀錄可共同運作。
