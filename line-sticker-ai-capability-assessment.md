# LINE 貼圖 AI 能力對照與加入評估

## 結論

**可以做到，而且目前網站已經具備核心 AI 能力的基礎。**目前版本已能在伺服器端執行真正 AI 圖像生成、AI 語意去背與多輪聊天修改；若要接近「如何製作LINE貼圖」參考任務的完整體驗，下一階段需要把單張創作能力擴展成可保存、可續作、可批次生成與可匯出的貼圖專案工作流。

需要區分兩個目標。第一個目標是「在網站內產出一套可下載的貼圖素材」，可完整實作。第二個目標是「保證一次生成 40 張都具有完全一致的角色、文字與構圖品質」，仍需要批次生成策略、角色設定檔、品質檢查與人工重試；AI 生成本身不應承諾每張都一次成功。

## 參考任務中觀察到的功能

| 功能 | 參考任務表現 | 目前網站狀態 | 加入建議 |
|---|---|---|---|
| 需求對話與方案規劃 | 先詢問張數、風格、主題、角色與文字情境，沒有想法時代為規劃 | 已有簡單聊天修改，尚無完整需求訪談 | **P0：加入建立專案精靈** |
| 多張角色照片上傳 | 使用者提供多張兔子照片，AI 綜合角色特徵 | 目前工作室以單張照片為主 | **P0：支援 1–6 張參考照片** |
| 角色設定檔 | 將兩隻兔子分別命名並記錄外觀特徵，再固定用於後續生成 | 尚無持久化角色設定檔 | **P0：建立角色卡與角色特徵摘要** |
| 貼圖張數選擇 | 可規劃 8／16／24／32／40 張，並列出每張文字與情境 | 尚無貼圖組規模與清單管理 | **P0：建立張數選擇與貼圖腳本表** |
| 文字與情境設計 | 先規劃「早安、謝謝、收到、加油」等日常語句，再進入圖像製作 | 目前每次生成只有單一情緒／單句 | **P0：加入批次文字與情境編輯器** |
| 角色一致性 | 多輪生成都要求維持角色特徵、風格與繁體中文 | 目前依賴每次 prompt 與當前圖片，沒有角色記憶 | **P0：每張生成都注入角色設定與風格鎖定** |
| 獨立貼圖生成 | 每張輸出為獨立 PNG，而不是把 40 張合併成一張 | 目前結果以單張卡片呈現 | **P0：建立貼圖組網格與單張狀態** |
| 批次生成與續作 | 先完成 1–15 張，配額恢復後再從第 16 張繼續，不重做既有內容 | 尚無批次佇列、進度保存與續作 | **P0：加入批次佇列、進度與重試** |
| 透明背景 | 參考任務要求每張獨立貼圖具透明背景 | 已有 AI 去背、PNG alpha 檢查與 JPEG／WEBP 轉 PNG 後處理 | **已具備；P1 可增加邊緣品質預覽** |
| 品質檢查 | 生成後檢查角色、文字、格式與透明背景 | 已有基本生成狀態，但尚無專案級檢查報告 | **P1：加入文字／尺寸／透明度檢查** |
| 對話式修改 | 使用者可要求重新設計或指定某張貼圖修改 | 已具備真正 AI 多輪 refine | **已具備；P1 擴充到指定貼圖與歷史版本** |
| ZIP 匯出 | 最後整理 40 個 PNG 與 ZIP 套件 | 目前有單張與全部下載 UI，但尚無伺服器 ZIP 產製 | **P1：加入 ZIP 打包與下載** |
| 檔名與編號 | 依序輸出 sticker_01_早安.png 等可辨識檔名 | 目前結果以前端索引下載 | **P1：加入編號、檔名編輯與排序** |
| 額度／失敗續接 | 圖像生成額度不足時保留已完成進度，稍後繼續 | 尚無持久化批次狀態 | **P0：保存每張狀態與失敗重試** |

## 與目前網站的技術落差

目前網站的前端工作室、tRPC 後端路由、S3 儲存、真正 AI 生成、語意去背與多輪修改已經存在，因此不需要重做核心 AI 服務。主要缺口是「產品化的貼圖組管理」：需要資料模型保存專案、角色、腳本與每張結果；需要伺服器端佇列逐張生成；需要使用者可查看進度、重試失敗項目、修改單張並下載完整套件。

| 項目 | 前端依賴 | 後端／服務依賴 | 儲存需求 |
|---|---|---|---|
| 專案精靈 | 表單、步驟導覽、角色卡 UI | 需求摘要與 prompt 組裝 | 專案與角色設定 |
| 批次生成 | 進度網格、取消／重試、輪詢或即時狀態 | 逐張呼叫圖片模型、錯誤重試與限流 | 每張圖片、狀態與 prompt |
| 角色一致性 | 角色鎖定提示與參考圖管理 | 角色設定注入、必要時使用多張參考圖 | 原始照片與角色摘要 |
| 透明與規格 | 檢查標籤、預覽棋盤格 | sharp／PNG 處理、尺寸與 alpha 檢查 | 處理後 PNG |
| ZIP 匯出 | 下載按鈕、匯出進度 | ZIP 產製與檔名清單 | 暫存 ZIP 與有效期限 |

## 成本與風險

真正的主要成本來自圖片模型呼叫次數。40 張貼圖若每張只生成一次需要約 40 次圖片請求；若每張平均重試或修改一次，實際請求數可能接近 80 次。因此建議先提供 4 張或 8 張小批次預覽，使用者確認角色與風格後再生成完整貼圖組，避免大量重做。

| 風險 | 影響 | 建議控制方式 |
|---|---|---|
| 角色在批次中漂移 | 40 張看起來不像同一角色 | 固定角色卡、參考圖、風格描述與負面限制；每批先做小樣本審核 |
| 中文文字錯字或變形 | 貼圖不可直接使用 | 文字先以腳本保存，生成後做 OCR／人工確認；必要時改為 AI 生成圖＋前端文字疊加 |
| 生成額度或服務失敗 | 批次中斷 | 每張獨立保存狀態，支援續作、重試與失敗原因 |
| 去背邊緣不乾淨 | 白邊、透明錯誤或角色細節遺失 | 棋盤格預覽、alpha 檢查、邊緣放大與人工重試 |
| 大型 ZIP 或暫存檔 | 下載失敗或儲存浪費 | 使用 S3 暫存、設定有效期限、完成後清理 |
| 直接宣稱符合 LINE 審核 | 使用者誤以為一定能上架 | 顯示「規格檢查」而非「保證審核通過」，保留人工審核步驟 |

## 建議分階段

**第一階段是貼圖組 MVP。**加入專案精靈、多張參考照片、角色卡、張數選擇、文字／情境清單、4 張小批次生成與貼圖網格。這一階段最能快速提升目前網站的完整度，也能沿用已存在的生成、去背與聊天修改路由。

**第二階段是批次生產與可恢復流程。**加入 8／16／24／32／40 張批次、逐張進度、失敗重試、續作、指定某張修改、歷史版本與角色一致性提示。這一階段才會接近參考任務中「先完成前 15 張、稍後從第 16 張接續」的體驗。

**第三階段是交付與規格工具。**加入檔名與編號、拖曳排序、尺寸與透明度檢查、ZIP 匯出、完整度報告與下載前檢查。完成後網站能從「生成單張」提升為「完成一套可整理的貼圖素材」。

## 建議結論

建議先確認加入**第一階段貼圖組 MVP**。在這個階段完成後，使用者可以上傳多張角色照片、建立角色設定、選擇 4／8 張預覽、編輯每張文字情境，再批次生成並逐張修改；確認角色一致性與生成品質後，再投入 40 張批次與 ZIP 匯出，能有效降低 AI 請求浪費與返工風險。
